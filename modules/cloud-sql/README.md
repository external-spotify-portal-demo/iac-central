# Cloud SQL PostgreSQL Module

Terraform module for provisioning a Cloud SQL PostgreSQL instance on GCP.

Includes automated backups, point-in-time recovery, and a maintenance window.

Configurable instance tier, disk size, and database name via input variables.

See `variables.tf` for all available configuration options.
